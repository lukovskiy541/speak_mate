# 📚 Технічна документація системи SpeakMate

## 1. Архітектура системи (MVT Pattern)

Проект SpeakMate побудований на базі фреймворку Django та використовує архітектурний патерн **MVT (Model-View-Template)**. Цей патерн забезпечує чітке розділення логіки, даних та представлення.

### Схема архітектури

```mermaid
graph TD
    User[Користувач] -->|HTTP Request| URL[URL Dispatcher]
    URL -->|Routing| View[View (Business Logic)]
    
    subgraph MVT Architecture
        View <-->|Read/Write| Model[Model (Data Layer)]
        Model <-->|SQL| DB[(Database)]
        View -->|Context| Template[Template (Presentation)]
    end
    
    Template -->|HTML Response| User
```

### Компоненти системи

| Компонент | Роль в SpeakMate | Приклади файлів |
|-----------|------------------|-----------------|
| **Model** | Описує структуру даних (користувачі, мови, цілі) та бізнес-правила. | `users/models.py` |
| **View** | Обробляє запити, взаємодіє з моделями, керує формами. | `users/views.py` |
| **Template** | Генерує HTML сторінки для користувача. | `templates/users/*.html` |
| **URL** | Маршрутизує запити до відповідних View. | `users/urls.py` |

---

## 2. Опис ендпоінтів (URLs та Views)

Система надає наступні інтерфейси для взаємодії з користувачем. Всі ендпоінти вимагають авторизації (`@login_required`).

### Таблиця маршрутів

| Метод | URL | View Function | Опис |
|-------|-----|---------------|------|
| `GET` | `/profile/` | `profile_view` | Перегляд профілю користувача |
| `GET` | `/profile/edit/` | `profile_edit` | Форма редагування профілю |
| `POST` | `/profile/edit/` | `profile_edit` | Збереження змін профілю |
| `GET` | `/profile/add-language/` | `add_language` | Форма додавання мови |
| `POST` | `/profile/add-language/` | `add_language` | Збереження нової мови |

---

## 3. Опис запитів та відповідей

Детальний опис логіки обробки запитів для кожного View.

### 3.1. Перегляд профілю (`profile_view`)
*   **URL:** `/profile/`
*   **Метод:** `GET`
*   **Вхідні дані:** `request.user` (з сесії)
*   **Логіка:** Отримує об'єкт поточного користувача.
*   **Відповідь:** Рендеринг шаблону `users/profile.html`.
*   **Контекст шаблону:**
    ```python
    {'user': UserObject}
    ```

### 3.2. Редагування профілю (`profile_edit`)
*   **URL:** `/profile/edit/`
*   **Методи:** `GET`, `POST`

#### GET Запит
*   **Логіка:** Створює форму `UserProfileForm`, заповнену даними користувача.
*   **Відповідь:** Шаблон `users/profile_edit.html`.

#### POST Запит
*   **Вхідні дані:** Form data (`first_name`, `last_name`, `bio`, `avatar`, etc.)
*   **Логіка:**
    1. Валідація даних формою `UserProfileForm`.
    2. Якщо валідно: збереження (`form.save()`), повідомлення про успіх, редірект на `/profile/`.
    3. Якщо не валідно: повторний рендеринг форми з помилками.
*   **Відповідь:** Redirect (302) або HTML з помилками (200).

### 3.3. Додавання мови (`add_language`)
*   **URL:** `/profile/add-language/`
*   **Методи:** `GET`, `POST`

#### GET Запит
*   **Логіка:** Створює порожню форму `UserLanguageForm`.
*   **Відповідь:** Шаблон `users/add_language.html`.

#### POST Запит
*   **Вхідні дані:**
    *   `language`: ID мови
    *   `proficiency`: Рівень (A1-C2)
*   **Логіка:**
    1. Валідація даних.
    2. Прив'язка мови до поточного користувача (`user_language.user = request.user`).
    3. Збереження в БД.
    4. Редірект на `/profile/`.
*   **Відповідь:** Redirect (302) або HTML з помилками (200).

---

## 4. Формати даних (Forms)

Система використовує Django Forms для валідації та обробки вхідних даних.

### 4.1. `UserProfileForm`
Використовується для редагування основного профілю.

| Поле | Тип | Обов'язкове | Опис |
|------|-----|-------------|------|
| `avatar` | Image | Ні | Фото профілю |
| `first_name` | Text | Ні | Ім'я |
| `last_name` | Text | Ні | Прізвище |
| `bio` | Textarea | Ні | Про себе |
| `birth_date` | Date | Ні | Дата народження (віджет: date picker) |
| `native_language` | Select | Так | Рідна мова (ForeignKey) |
| `timezone` | Select | Так | Часовий пояс |
| `goals` | Checkbox | Ні | Цілі вивчення (Many-to-Many) |
| `interests` | Checkbox | Ні | Інтереси (Many-to-Many) |

### 4.2. `UserLanguageForm`
Використовується для додавання мов, які вивчає користувач.

| Поле | Тип | Обов'язкове | Валідація |
|------|-----|-------------|-----------|
| `language` | Select | Так | Має бути існуюча мова з БД |
| `proficiency` | Select | Так | Один з варіантів: A1, A2, B1, B2, C1, C2 |

---

## 5. Приклади використання (Use Cases)

### Сценарій 1: Налаштування профілю після реєстрації
1.  **Користувач** переходить на `/profile/edit/`.
2.  **Система** відображає форму з поточними даними.
3.  **Користувач** завантажує аватар, обирає рідну мову "Ukrainian" та ціль "Travel".
4.  **Користувач** натискає "Зберегти".
5.  **Система** валідує дані, зберігає їх в БД та перенаправляє на `/profile/`.
6.  **Користувач** бачить оновлений профіль.

### Сценарій 2: Додавання нової мови для вивчення
1.  **Користувач** натискає "Додати мову" на сторінці профілю.
2.  **Система** відкриває сторінку `/profile/add-language/`.
3.  **Користувач** обирає "English" та рівень "B2".
4.  **Користувач** відправляє форму.
5.  **Система** створює запис в таблиці `UserLanguage`, пов'язуючи користувача з мовою.
6.  **Система** перенаправляє на профіль, де в списку мов з'являється "English (B2)".

---

*Документація актуальна для версії системи станом на 26.11.2025.*
